# node_modules (필수 - 의존성 폴더)
node_modules/

# 환경 변수 파일
.env

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 운영체제별 불필요한 파일
.DS_Store         # macOS
Thumbs.db         # Windows

# VS Code 설정 파일 (선택적 무시)
.vscode/

# 빌드 결과물
dist/
build/

# 테스트 커버리지 결과
coverage/

# 잠금 파일 (선택적으로 무시 가능)
package-lock.json
yarn.lock
